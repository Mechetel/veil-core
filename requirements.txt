# Web + task framework
fastapi
uvicorn[standard]
pydantic-settings
celery
redis
httpx

# Config / data
pyyaml
# Pinned to the dissertation's proven stack (NumPy 1.x — newer scikit-image/
# tifffile require NumPy>=2.1 which breaks the torch/skimage 1.x-compiled wheels).
numpy==1.26.4
pillow
imageio==2.34.0
scikit-image==0.22.0
reedsolo  # Reed-Solomon ECC used by steganogan/utils/crypto.py (vendored)
tqdm

# NOTE: torch + torchvision are installed separately (CPU vs CUDA wheel) via the
# Dockerfile / dev command:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/${TORCH_INDEX:-cpu}
