# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Model weights — synced separately to a persistent volume (see
# scripts/collect_weights.py + scripts/sync_weights_to_server.sh). Not baked into images.
/weights/

# OS / editor
.DS_Store
