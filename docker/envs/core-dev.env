# Non-secret dev config (secrets/tokens live in secret-envs/veil-core.env).
REDIS_URL=redis://redis:6379/0
# Web runs on the host (bin/dev); reach it from inside the container:
WEB_CALLBACK_URL=http://host.docker.internal:3000
WEIGHTS_DIR=/app/weights
CUDA=false
